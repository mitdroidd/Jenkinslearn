# ---- EDIT ME before each deploy to see the pipeline pick up your change ----
app.message=Hello from the backend, build v1!
app.version=1.0.0
# -----------------------------------------------------------------------------

# Context path when deployed as a WAR named greeting-app.war, Tomcat serves
# this app at http://localhost:8081/greeting-app/api/greeting
server.servlet.context-path=/
